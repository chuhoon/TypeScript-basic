{
  "compilerOptions": {
    "allowJs": true, // 자바스크립트까지 검증
    "target": "ES5", // 해당 문법까지
    "outDir": "./built",
    "moduleResolution": "Node" // Promise 관련 설정
  },
  "include": ["./src/**/*"] // 프로젝트를 기준으로 해서 어떤 폴더를 대상으로 컴파일할건지
}
